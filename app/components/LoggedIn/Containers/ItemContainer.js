import React, { Component } from "react";
import { Text, StyleSheet, View } from "react-native";

export default class ItemContainer extends Component {
  render() {
    return (
      <View style={{ flex: 1 }}>
        <Text> container of im </Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({});
